# Copy this file to .env
# Currently no required env vars — Kokomi is fully local
# Future use: web dashboard port, custom data paths

DEBUG=false
DATA_DIR=./data
DB_PATH=./data/kokomi.db
CHECKPOINT_DIR=./data/checkpoints
