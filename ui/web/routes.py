"""
Web API routes.
Stage 4 implementation target.

Planned endpoints:
    GET  /api/projects        → list projects
    POST /api/projects        → add project
    GET  /api/tasks/<project> → list tasks
    POST /api/tasks           → add task
    POST /api/chat            → send message to Kokomi
    GET  /api/memory          → list memories
"""
# TODO Stage 4: implement routes
